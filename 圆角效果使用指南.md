# 圆角效果快速使用指南

## 🚀 快速开始

### 1. 基础使用

```javascript
const RoundedCornerService = require('./src/services/RoundedCornerService.cjs');
const service = new RoundedCornerService();

// 应用圆角到视频
const result = await service.applyRoundedCornersToVideo(
  'input.mp4',
  20,  // 圆角半径
  {
    width: 360,
    height: 640,
    borderWidth: 4,
    borderColor: 'white',
    shadow: {
      enabled: true,
      offsetX: 2,
      offsetY: 6,
      blur: 4,
      opacity: 0.3
    }
  }
);

console.log('生成的视频:', result);
```

### 2. 在视频合成中使用

```javascript
import ServerVideoCompositionService from './src/services/ServerVideoCompositionService.js';

const service = new ServerVideoCompositionService();

// 画中画视频会自动应用圆角和阴影
const result = await service.overlayFaceVideoPIP(
  backgroundVideo,
  faceVideo,
  'douyin',  // 平台：douyin 或 kuaishou
  'bottom'   // 位置：bottom, topRight, center
);
```

### 3. 使用预设

```javascript
const { getRoundedCornerPreset } = require('./src/utils/roundedCornerHelpers.js');

// 获取抖音画中画预设
const preset = getRoundedCornerPreset('pip_douyin');

const result = await service.applyRoundedCornersToVideo(
  'input.mp4',
  preset.radius,
  {
    width: 360,
    height: 640,
    ...preset
  }
);
```

## 📋 可用预设

| 预设名称 | 圆角 | 边框 | 阴影 | 适用场景 |
|---------|------|------|------|---------|
| `small` | 4px | 2px | 无 | 小元素 |
| `medium` | 8px | 3px | 轻微 | 标准卡片 |
| `large` | 12px | 4px | 标准 | 大卡片 |
| `xlarge` | 20px | 4px | 标准 | 特殊设计 |
| `circle` | 9999px | 4px | 轻微 | 圆形头像 |
| `pip_douyin` | 20px | 4px | 标准 | 抖音画中画 |
| `pip_kuaishou` | 20px | 4px | 标准 | 快手画中画 |
| `none` | 0px | 4px | 无 | 仅边框 |

## 🎨 配置选项

### 基础配置

```javascript
{
  width: 360,           // 视频宽度（必需）
  height: 640,          // 视频高度（必需）
  borderWidth: 4,       // 边框宽度（默认4）
  borderColor: 'white', // 边框颜色（默认white）
  useVP9: true,         // 使用VP9编码器（默认true）
  antiAlias: true       // 抗锯齿（默认true）
}
```

### 阴影配置

```javascript
{
  shadow: {
    enabled: true,      // 启用阴影（默认false）
    offsetX: 2,         // X偏移（默认2）
    offsetY: 6,         // Y偏移（默认6）
    blur: 4,            // 模糊半径（默认4）
    opacity: 0.3,       // 不透明度0-1（默认0.3）
    color: '#000000'    // 阴影颜色（默认黑色）
  }
}
```

## 🧪 测试

### 运行测试

```bash
cd vidslide-ai
node test-rounded-simple.cjs
```

### 查看生成的视频

```bash
open output/rounded_*.webm
```

## 📊 性能建议

### 编码器选择

- **VP9** (推荐): 支持透明圆角和阴影，效果最佳
  - 处理时间: 约为原视频时长的1.5-2倍
  - 文件格式: .webm
  - 适用: 需要高质量圆角效果

- **H.264** (降级): 速度快但不支持透明度
  - 处理时间: 约为原视频时长的0.8-1倍
  - 文件格式: .mp4
  - 适用: 快速处理或不需要透明度

### 缓存机制

- 蒙版自动缓存，相同尺寸和圆角半径会复用
- 缓存位置: `vidslide-ai/cache/masks/`
- 清理缓存: `service.maskGenerator.clearCache()`

## 🔧 故障排除

### 问题1: 圆角不可见

**原因**: 可能使用了H.264编码器
**解决**: 确保 `useVP9: true`

```javascript
{
  useVP9: true  // 必须使用VP9才能支持透明圆角
}
```

### 问题2: 处理速度慢

**原因**: VP9编码比H.264慢
**解决**:
- 短视频使用VP9（效果优先）
- 长视频考虑H.264（速度优先）
- 或使用批量处理

```javascript
// 批量处理
const results = await service.batchProcessVideos(
  [video1, video2, video3],
  20,
  options
);
```

### 问题3: 阴影效果不明显

**原因**: 阴影参数太小
**解决**: 增加模糊半径和不透明度

```javascript
{
  shadow: {
    enabled: true,
    blur: 8,        // 增加模糊
    opacity: 0.5    // 增加不透明度
  }
}
```

## 📝 常见用例

### 用例1: 抖音画中画

```javascript
const result = await service.applyRoundedCornersToVideo(
  faceVideo,
  20,
  {
    width: 360,
    height: 640,
    borderWidth: 4,
    borderColor: 'white',
    shadow: {
      enabled: true,
      offsetX: 2,
      offsetY: 6,
      blur: 4,
      opacity: 0.3
    },
    useVP9: true
  }
);
```

### 用例2: 圆形头像

```javascript
const result = await service.applyRoundedCornersToVideo(
  avatarVideo,
  180,  // 半径=宽度/2，形成圆形
  {
    width: 360,
    height: 360,  // 正方形
    borderWidth: 4,
    borderColor: 'white',
    shadow: {
      enabled: true,
      blur: 4,
      opacity: 0.25
    }
  }
);
```

### 用例3: 无阴影圆角

```javascript
const result = await service.applyRoundedCornersToVideo(
  video,
  12,
  {
    width: 360,
    height: 640,
    borderWidth: 3,
    borderColor: 'white',
    shadow: { enabled: false },  // 禁用阴影
    useVP9: true
  }
);
```

## 🎯 最佳实践

1. **使用预设**: 优先使用预设配置，保持一致性
2. **缓存利用**: 相同尺寸和圆角半径会自动复用缓存
3. **编码器选择**: 需要透明度时使用VP9，否则H.264更快
4. **阴影适度**: 阴影不透明度建议0.2-0.4之间
5. **圆角适中**: 圆角半径建议不超过宽度的1/4

## 📚 更多信息

- 完整文档: [圆角效果实施完成报告.md](./圆角效果实施完成报告.md)
- 测试文件: `vidslide-ai/test-rounded-simple.cjs`
- 源代码: `vidslide-ai/src/services/RoundedCornerService.cjs`

# VidSlide AI - 真实视频测试执行计划

> **使用真实视频逐步验证各个智能体的工作表现**
>
> 测试视频: ~/Desktop/测试视频2.mp4 (140MB)
> 测试时间: 2026-01-23
> 目标: 验证"原视频+卡片"效果，发现并修复问题

---

## 📋 测试总览

### 测试策略
- **渐进式测试**: 一次测试一个智能体，确保每个环节都正常工作
- **问题记录**: 详细记录每个智能体的表现和遇到的问题
- **及时修复**: 发现问题立即修复，再继续下一步
- **数据保存**: 保存每个阶段的中间结果，便于调试

### 测试环境
```
测试视频: ~/Desktop/测试视频2.mp4
输出目录: ./real-test-output/
日志文件: ./real-test-output/test.log
```

---

## 🎯 阶段1: 环境准备和视频检查

### 目标
- 确认测试视频可用
- 检查API配置
- 准备测试环境

### 测试步骤

#### 1.1 检查测试视频信息
```bash
# 获取视频基本信息
ffprobe -v quiet -print_format json -show_format -show_streams ~/Desktop/测试视频2.mp4

# 检查项:
- 视频时长
- 分辨率
- 编码格式
- 音频轨道
```

#### 1.2 检查API配置
```bash
# 检查API密钥配置
node scripts/check-api-keys.js

# 需要检查:
- 百度ASR API密钥
- 文心一言API密钥
- 豆包API密钥
```

#### 1.3 准备测试环境
```bash
# 创建输出目录
mkdir -p real-test-output/{phase1,phase2,phase3,phase4,phase5,logs}

# 复制测试视频到工作目录
cp ~/Desktop/测试视频2.mp4 ./real-test-output/test-video.mp4
```

### 预期结果
- ✅ 视频信息正常
- ✅ API配置完整
- ✅ 测试环境就绪

### 问题记录
```
问题1: [待记录]
解决方案: [待记录]
```

---

## 🎯 阶段2: 测试ContentAnalyst（内容理解智能体）

### 目标
验证内容理解智能体能否正确：
1. 提取视频中的语音文字
2. 调用文心一言进行深度分析
3. 生成准确的理解文档

### 测试步骤

#### 2.1 测试语音识别（百度ASR）
```javascript
// 创建测试脚本: test-phase1-asr.js
import BaiduASRService from './src/services/BaiduASRService.js';

const asr = new BaiduASRService();
const videoPath = './real-test-output/test-video.mp4';

console.log('开始语音识别...');
const transcript = await asr.recognize(videoPath);

console.log('识别结果:');
console.log(transcript);

// 保存结果
fs.writeFileSync(
  './real-test-output/phase1/transcript.json',
  JSON.stringify({ transcript }, null, 2)
);
```

**检查项:**
- [ ] 语音识别是否成功
- [ ] 识别准确率如何
- [ ] 是否有乱码或错误
- [ ] 识别速度是否可接受

#### 2.2 测试文心一言分析
```javascript
// 创建测试脚本: test-phase1-wenxin.js
import WenxinService from './src/services/WenxinService.js';

const wenxin = new WenxinService();
const transcript = JSON.parse(
  fs.readFileSync('./real-test-output/phase1/transcript.json')
).transcript;

console.log('开始文心一言分析...');
const analysis = await wenxin.analyzeContent({
  text: transcript,
  tasks: [
    'extract_key_points',
    'extract_keywords',
    'identify_viewpoints',
    'identify_explanations'
  ]
});

console.log('分析结果:');
console.log(JSON.stringify(analysis, null, 2));

// 保存结果
fs.writeFileSync(
  './real-test-output/phase1/analysis.json',
  JSON.stringify(analysis, null, 2)
);
```

**检查项:**
- [ ] 关键词提取是否准确
- [ ] 观点识别是否合理
- [ ] 解释内容是否正确分类
- [ ] 分析结果是否符合预期

#### 2.3 测试完整的ContentAnalyst
```javascript
// 创建测试脚本: test-phase1-full.js
import ContentAnalyst from './src/agents/executors/ContentAnalyst.js';
import Logger from './src/core/Logger.js';

const analyst = new ContentAnalyst({
  logger: new Logger({ level: 'debug' })
});

const videoPath = './real-test-output/test-video.mp4';

console.log('开始完整内容分析...');
const understanding = await analyst.analyze(videoPath);

console.log('理解文档:');
console.log(JSON.stringify(understanding, null, 2));

// 保存结果
fs.writeFileSync(
  './real-test-output/phase1/understanding.json',
  JSON.stringify(understanding, null, 2)
);
```

**检查项:**
- [ ] 理解文档结构完整
- [ ] 包含所有必要字段
- [ ] 数据质量符合要求
- [ ] 可以作为下一阶段输入

### 预期输出
```json
{
  "transcript": "完整的语音识别文字...",
  "keyPoints": ["重点1", "重点2", "重点3"],
  "keywords": ["关键词1", "关键词2"],
  "viewpoints": [
    {
      "text": "观点内容",
      "type": "main",
      "time": { "start": 0, "end": 5 }
    }
  ],
  "explanations": [
    {
      "text": "解释内容",
      "keyword": "关键词",
      "time": { "start": 5, "end": 10 }
    }
  ]
}
```

### 问题记录
```
问题1: [待记录]
智能体表现评分: [待评分 1-10]
改进建议: [待记录]
```

---

## 🎯 阶段3: 测试SceneDesigner（场景设计智能体）

### 目标
验证场景设计智能体能否：
1. 根据理解文档正确拆解场景
2. 合理分配场景类型（原视频+卡片 vs 多层组合）
3. 生成无冲突的时间轴

### 测试步骤

#### 3.1 测试场景拆解
```javascript
// 创建测试脚本: test-phase2-scenes.js
import SceneDesigner from './src/agents/executors/SceneDesigner.js';
import Logger from './src/core/Logger.js';

const designer = new SceneDesigner({
  logger: new Logger({ level: 'debug' })
});

// 读取阶段1的理解文档
const understanding = JSON.parse(
  fs.readFileSync('./real-test-output/phase1/understanding.json')
);

// 获取视频时长
const videoDuration = 60; // 从ffprobe获取

console.log('开始场景设计...');
const scenes = await designer.decomposeScenes({
  understanding,
  videoDuration
});

console.log('场景方案:');
console.log(JSON.stringify(scenes, null, 2));

// 保存结果
fs.writeFileSync(
  './real-test-output/phase2/scenes.json',
  JSON.stringify(scenes, null, 2)
);
```

**检查项:**
- [ ] 场景数量是否合理
- [ ] 场景类型分配是否正确
- [ ] 时间轴是否有重叠
- [ ] 卡片内容是否匹配关键词
- [ ] 场景时长是否合适（3-8秒）

#### 3.2 验证时间轴
```javascript
// 时间轴验证脚本
function validateTimeline(scenes) {
  const issues = [];

  // 检查重叠
  for (let i = 0; i < scenes.length - 1; i++) {
    const current = scenes[i];
    const next = scenes[i + 1];

    if (current.endTime > next.startTime) {
      issues.push({
        type: 'overlap',
        scene1: current.id,
        scene2: next.id,
        overlap: current.endTime - next.startTime
      });
    }
  }

  // 检查间隙
  for (let i = 0; i < scenes.length - 1; i++) {
    const current = scenes[i];
    const next = scenes[i + 1];

    if (current.endTime < next.startTime) {
      issues.push({
        type: 'gap',
        scene1: current.id,
        scene2: next.id,
        gap: next.startTime - current.endTime
      });
    }
  }

  return issues;
}
```

### 预期输出
```json
{
  "scenes": [
    {
      "id": "scene_1",
      "type": "video-with-card",
      "startTime": 5,
      "endTime": 10,
      "cardText": "关键词1",
      "cardSubtitle": "Keyword 1",
      "cardStyle": "blue"
    },
    {
      "id": "scene_2",
      "type": "video-with-card",
      "startTime": 10,
      "endTime": 15,
      "cardText": "关键词2",
      "cardSubtitle": "Keyword 2",
      "cardStyle": "yellow"
    }
  ],
  "timeline": {
    "totalDuration": 60,
    "sceneCount": 2
  }
}
```

### 问题记录
```
问题1: [待记录]
智能体表现评分: [待评分 1-10]
改进建议: [待记录]
```

---

## 🎯 阶段4: 测试VisualDesigner（视觉设计智能体）

### 目标
验证视觉设计智能体能否：
1. 生成美观的卡片图片
2. 正确应用蓝色/黄色样式
3. 文字排版清晰可读

### 测试步骤

#### 4.1 测试卡片生成
```javascript
// 创建测试脚本: test-phase3-cards.js
import VisualDesigner from './src/agents/executors/VisualDesigner.js';
import Logger from './src/core/Logger.js';

const designer = new VisualDesigner({
  logger: new Logger({ level: 'debug' })
});

// 读取场景方案
const scenes = JSON.parse(
  fs.readFileSync('./real-test-output/phase2/scenes.json')
);

console.log('开始卡片设计...');
const cards = await designer.designCards({ scenes: scenes.scenes });

console.log('卡片资源:');
console.log(JSON.stringify(cards, null, 2));

// 保存结果
fs.writeFileSync(
  './real-test-output/phase3/cards.json',
  JSON.stringify(cards, null, 2)
);
```

**检查项:**
- [ ] 卡片图片是否生成成功
- [ ] 卡片尺寸是否正确（450x300）
- [ ] 蓝色/黄色样式是否正确
- [ ] 文字是否清晰可读
- [ ] 中英文排版是否美观

#### 4.2 手动检查卡片质量
```bash
# 打开生成的卡片图片
open ./real-test-output/phase3/cards/*.png

# 检查清单:
- 背景颜色是否正确
- 文字大小是否合适
- 圆角和阴影效果
- 整体视觉效果
```

### 预期输出
```json
{
  "cards": [
    {
      "id": "card_1",
      "path": "./real-test-output/phase3/cards/card_keyword1.png",
      "text": "关键词1",
      "subtitle": "Keyword 1",
      "style": "blue",
      "size": { "width": 450, "height": 300 }
    },
    {
      "id": "card_2",
      "path": "./real-test-output/phase3/cards/card_keyword2.png",
      "text": "关键词2",
      "subtitle": "Keyword 2",
      "style": "yellow",
      "size": { "width": 450, "height": 300 }
    }
  ]
}
```

### 问题记录
```
问题1: [待记录]
智能体表现评分: [待评分 1-10]
改进建议: [待记录]
```

---

## 🎯 阶段5: 测试VideoEngineer（视频工程师）

### 目标
验证视频工程师能否：
1. 正确使用FFmpeg合成视频
2. 准确叠加卡片到原视频
3. 输出符合规格的视频（1080x1920）

### 测试步骤

#### 5.1 测试单个场景合成
```javascript
// 创建测试脚本: test-phase4-single-scene.js
import VideoEngineer from './src/agents/executors/VideoEngineer.js';
import Logger from './src/core/Logger.js';

const engineer = new VideoEngineer({
  logger: new Logger({ level: 'debug' })
});

// 读取场景和卡片数据
const scenes = JSON.parse(
  fs.readFileSync('./real-test-output/phase2/scenes.json')
);
const cards = JSON.parse(
  fs.readFileSync('./real-test-output/phase3/cards.json')
);

// 测试第一个场景
const scene = scenes.scenes[0];
const card = cards.cards[0];

console.log('测试单个场景合成...');
const output = await engineer.composeSingleScene({
  videoPath: './real-test-output/test-video.mp4',
  scene: scene,
  card: card,
  outputPath: './real-test-output/phase4/scene_1.mp4'
});

console.log('场景合成完成:', output);
```

**检查项:**
- [ ] FFmpeg命令是否正确
- [ ] 视频是否成功生成
- [ ] 卡片位置是否正确
- [ ] 卡片是否清晰可见
- [ ] 视频时长是否正确

#### 5.2 测试完整视频合成
```javascript
// 创建测试脚本: test-phase4-full-video.js
import VideoEngineer from './src/agents/executors/VideoEngineer.js';
import Logger from './src/core/Logger.js';

const engineer = new VideoEngineer({
  logger: new Logger({ level: 'debug' })
});

// 读取所有数据
const scenes = JSON.parse(
  fs.readFileSync('./real-test-output/phase2/scenes.json')
);
const cards = JSON.parse(
  fs.readFileSync('./real-test-output/phase3/cards.json')
);

console.log('开始完整视频合成...');
const finalVideo = await engineer.composeVideo({
  videoPath: './real-test-output/test-video.mp4',
  scenes: scenes.scenes,
  cards: cards.cards,
  outputPath: './real-test-output/phase4/final-video.mp4'
});

console.log('视频合成完成:', finalVideo);

// 保存结果
fs.writeFileSync(
  './real-test-output/phase4/video-info.json',
  JSON.stringify(finalVideo, null, 2)
);
```

**检查项:**
- [ ] 所有场景是否正确合成
- [ ] 场景切换是否流畅
- [ ] 卡片出现时机是否准确
- [ ] 视频分辨率是否正确（1080x1920）
- [ ] 音频是否保留
- [ ] 文件大小是否合理

#### 5.3 手动检查视频质量
```bash
# 播放生成的视频
open ./real-test-output/phase4/final-video.mp4

# 检查清单:
- 卡片是否在正确的时间出现
- 卡片位置是否合适
- 卡片是否清晰
- 原视频质量是否保持
- 音频是否正常
```

### 预期输出
```json
{
  "finalVideoPath": "./real-test-output/phase4/final-video.mp4",
  "duration": 60,
  "resolution": "1080x1920",
  "fileSize": "50MB",
  "scenes": 2,
  "cards": 2
}
```

### 问题记录
```
问题1: [待记录]
智能体表现评分: [待评分 1-10]
改进建议: [待记录]
```

---

## 🎯 阶段6: 测试QualityDirector（质量总监）

### 目标
验证质量总监能否：
1. 检测各种质量问题
2. 验证素材准确性
3. 生成详细的质量报告

### 测试步骤

#### 6.1 测试质量检查
```javascript
// 创建测试脚本: test-phase5-quality.js
import QualityDirector from './src/agents/quality/QualityDirector.js';
import Logger from './src/core/Logger.js';

const director = new QualityDirector({
  logger: new Logger({ level: 'debug' })
});

// 读取所有阶段的数据
const understanding = JSON.parse(
  fs.readFileSync('./real-test-output/phase1/understanding.json')
);
const scenes = JSON.parse(
  fs.readFileSync('./real-test-output/phase2/scenes.json')
);
const cards = JSON.parse(
  fs.readFileSync('./real-test-output/phase3/cards.json')
);
const video = JSON.parse(
  fs.readFileSync('./real-test-output/phase4/video-info.json')
);

console.log('开始质量检查...');
const qualityReport = await director.checkAll({
  understanding,
  scenes,
  cards,
  video
});

console.log('质量报告:');
console.log(JSON.stringify(qualityReport, null, 2));

// 保存结果
fs.writeFileSync(
  './real-test-output/phase5/quality-report.json',
  JSON.stringify(qualityReport, null, 2)
);
```

**检查项:**
- [ ] 错别字检查是否有效
- [ ] 素材准确性验证是否合理
- [ ] 时间轴冲突检测是否准确
- [ ] 质量评分是否合理
- [ ] 改进建议是否有用

### 预期输出
```json
{
  "passed": true,
  "score": 95,
  "checks": {
    "contentAccuracy": { "passed": true, "score": 95 },
    "cardQuality": { "passed": true, "score": 98 },
    "timelineValidity": { "passed": true, "score": 100 },
    "videoQuality": { "passed": true, "score": 90 }
  },
  "violations": [],
  "warnings": [],
  "suggestions": [
    "建议增加卡片动画效果",
    "建议优化卡片位置"
  ]
}
```

### 问题记录
```
问题1: [待记录]
智能体表现评分: [待评分 1-10]
改进建议: [待记录]
```

---

## 📊 最终测试报告

### 智能体表现评分

| 智能体 | 评分 (1-10) | 主要问题 | 改进建议 |
|--------|-------------|----------|----------|
| ContentAnalyst | [待评分] | [待记录] | [待记录] |
| SceneDesigner | [待评分] | [待记录] | [待记录] |
| VisualDesigner | [待评分] | [待记录] | [待记录] |
| VideoEngineer | [待评分] | [待记录] | [待记录] |
| QualityDirector | [待评分] | [待记录] | [待记录] |

### 整体测试结果

**成功率**: [待计算]%

**主要成就**:
- [待记录]

**主要问题**:
- [待记录]

**优先修复项**:
1. [待记录]
2. [待记录]
3. [待记录]

### 下一步行动

**立即修复** (P0):
- [ ] [待记录]

**优先改进** (P1):
- [ ] [待记录]

**未来优化** (P2):
- [ ] [待记录]

---

## 📝 测试执行记录

### 执行日志

```
[2026-01-23 开始测试]

阶段1: 环境准备
- [时间] [状态] [详情]

阶段2: ContentAnalyst测试
- [时间] [状态] [详情]

阶段3: SceneDesigner测试
- [时间] [状态] [详情]

阶段4: VisualDesigner测试
- [时间] [状态] [详情]

阶段5: VideoEngineer测试
- [时间] [状态] [详情]

阶段6: QualityDirector测试
- [时间] [状态] [详情]

[测试完成]
```

---

**文档维护**: 在测试过程中持续更新此文档
**最后更新**: 2026-01-23

# 🎯 VidSlide AI 卡片系统优化方案

**日期**: 2026-01-23
**目标**: 实现理想的卡片效果

---

## 📊 当前问题分析

### 问题1: 关键词提取错误 ❌
**现象**:
- 原文: "做获客要投放**巨量ad**"
- 提取: "质量ad"
- 正确: "巨量ad"

**根本原因**:
1. 文心一言Prompt不够精确
2. 没有强调"必须从原文精确提取"
3. 没有验证提取的关键词是否在原文中

### 问题2: 卡片内容错误 ❌
**现象**:
- 当前: 使用完整观点句子 "做获客要投放质量ad"
- 正确: 只显示关键词 "巨量ad" + 英文翻译

**根本原因**:
1. SceneDesigner使用观点文本作为cardText
2. VisualDesigner直接使用cardText生成卡片
3. 没有关键词到英文翻译的映射

### 问题3: 卡片样式不符合目标 ❌
**当前**:
- 尺寸: 800x200 (横版但太小)
- 位置: 右上角
- 样式: 纯色背景，无圆角
- 内容: 只有中文，无英文

**目标**:
- 尺寸: 更大的横版卡片 (建议 600x300)
- 位置: 视频底部居中
- 样式: 渐变背景，圆角，阴影
- 内容: 中文主标题 + 英文副标题

---

## 🔧 优化方案

### 方案A: 修改ContentAnalyst的Prompt（推荐）

#### 1. 优化关键词提取Prompt

**修改点**:
```javascript
// 原Prompt
1. 提取3-5个核心关键词
   - 必须是名词
   - 长度2-6个字
   - 代表核心概念

// 新Prompt
1. 提取3-5个核心关键词
   - 必须从原文中精确提取（不要改写或总结）
   - 必须是名词或名词短语
   - 长度2-6个字
   - 代表核心概念
   - 提供英文翻译
```

#### 2. 修改输出格式

**新增字段**:
```json
{
  "keywords": [
    {
      "text": "巨量ad",
      "english": "Massive AD",
      "category": "platform"
    },
    {
      "text": "强化学习",
      "english": "Reinforcement Learning",
      "category": "technology"
    }
  ]
}
```

### 方案B: 修改SceneDesigner逻辑

#### 1. 场景设计时使用关键词而非观点

**修改点**:
```javascript
// 原逻辑
if (content.type === 'viewpoint') {
  scenes.push({
    type: 'video-with-card',
    cardText: content.text,  // ❌ 使用观点文本
    cardStyle: content.importance === 'high' ? 'blue' : 'yellow'
  });
}

// 新逻辑
if (content.type === 'viewpoint') {
  // 从关键词中选择最相关的
  const relatedKeyword = this.findRelatedKeyword(content.text, keywords);
  scenes.push({
    type: 'video-with-card',
    keyword: relatedKeyword,  // ✅ 使用关键词对象
    cardStyle: content.importance === 'high' ? 'blue' : 'yellow'
  });
}
```

### 方案C: 重构VisualDesigner卡片生成

#### 1. 新的卡片设计规范

**尺寸**: 600x300 (横版)
**布局**:
```
┌─────────────────────────────────┐
│                                 │
│         巨量ad                  │  ← 中文主标题 (大字)
│      Massive AD                 │  ← 英文副标题 (小字)
│                                 │
└─────────────────────────────────┘
```

**样式**:
- 背景: 渐变 (蓝色: #2563eb → #1e40af, 黄色: #fbbf24 → #f59e0b)
- 圆角: 20px
- 阴影: 0 4px 6px rgba(0,0,0,0.1)
- 文字: 居中对齐

#### 2. 修改generateCardImage方法

```javascript
async generateCardImage(keyword, styleName, sceneId) {
  const style = this.cardStyles[styleName];
  const width = 600;   // 增大宽度
  const height = 300;  // 增大高度

  const canvas = createCanvas(width, height);
  const ctx = canvas.getContext('2d');

  // 1. 绘制渐变背景
  const gradient = ctx.createLinearGradient(0, 0, width, height);
  gradient.addColorStop(0, style.gradientStart);
  gradient.addColorStop(1, style.gradientEnd);
  ctx.fillStyle = gradient;

  // 2. 绘制圆角矩形
  this.roundRect(ctx, 0, 0, width, height, 20);
  ctx.fill();

  // 3. 绘制中文主标题
  ctx.fillStyle = style.text;
  ctx.font = 'bold 72px "Microsoft YaHei"';
  ctx.textAlign = 'center';
  ctx.textBaseline = 'middle';
  ctx.fillText(keyword.text, width / 2, height / 2 - 30);

  // 4. 绘制英文副标题
  ctx.font = '32px Arial';
  ctx.fillStyle = style.textSecondary;
  ctx.fillText(keyword.english, width / 2, height / 2 + 40);

  // 5. 保存
  const filepath = path.join(this.outputDir, `card_${sceneId}_${Date.now()}.png`);
  const buffer = canvas.toBuffer('image/png');
  fs.writeFileSync(filepath, buffer);

  return filepath;
}
```

### 方案D: 修改视频合成位置

#### 1. 修改ServerVideoCompositionService

**卡片叠加位置**:
```javascript
// 原位置: 右上角或居中
overlay=x=(W-w)/2:y=100

// 新位置: 底部居中
overlay=x=(W-w)/2:y=H-h-100
```

---

## 🎯 实施步骤

### 步骤1: 优化ContentAnalyst (最关键)

1. 修改`buildAnalysisPrompt`方法
2. 修改输出格式，keywords改为对象数组
3. 添加关键词验证逻辑

### 步骤2: 修改SceneDesigner

1. 修改场景设计逻辑，使用关键词而非观点
2. 添加关键词匹配方法

### 步骤3: 重构VisualDesigner

1. 修改卡片尺寸为600x300
2. 实现渐变背景
3. 实现圆角效果
4. 添加中英文双语显示

### 步骤4: 修改视频合成位置

1. 修改ServerVideoCompositionService的overlay位置
2. 确保卡片在底部居中

### 步骤5: 测试验证

1. 使用测试视频2.MP4重新测试
2. 验证关键词提取准确性
3. 验证卡片样式和位置
4. 对比理想效果图

---

## 📝 预期效果

### 关键词提取
```json
{
  "keywords": [
    {
      "text": "巨量ad",
      "english": "Massive AD",
      "category": "platform"
    },
    {
      "text": "强化学习",
      "english": "Reinforcement Learning",
      "category": "technology"
    },
    {
      "text": "涌现",
      "english": "Emergence",
      "category": "concept"
    }
  ]
}
```

### 卡片效果
- ✅ 尺寸: 600x300 横版
- ✅ 位置: 视频底部居中
- ✅ 样式: 渐变背景 + 圆角 + 阴影
- ✅ 内容: "巨量ad" (大字) + "Massive AD" (小字)
- ✅ 颜色: 蓝色/黄色渐变

### 视频效果
- ✅ 原视频作为背景
- ✅ 卡片在底部，不遮挡主要内容
- ✅ 卡片样式专业、精致
- ✅ 符合抖音短视频风格

---

## ⚠️ 注意事项

1. **关键词准确性**: 必须从原文精确提取，不能改写
2. **英文翻译**: 需要准确的专业术语翻译
3. **卡片尺寸**: 不能太大遮挡视频，也不能太小看不清
4. **渐变效果**: 需要测试不同颜色组合
5. **圆角实现**: Canvas需要自定义圆角矩形方法

---

## 🚀 下一步

1. 你确认这个方案吗？
2. 我们从哪个步骤开始？
3. 是否需要调整某些细节？

**建议**: 从步骤1开始，先优化ContentAnalyst的关键词提取，这是最关键的一步。

---

**文档创建时间**: 2026-01-23
**状态**: 待实施

# 简化流程测试指南

## ✅ 已完成的工作

### 1. 核心服务清理
- ✅ 删除了 30+ 个旧服务文件
- ✅ 保留了 4 个核心服务：
  - `MasterAutoGenerationAgent.js` - 主控制器
  - `DoubaoImageService.js` - 豆包生图
  - `MicroSceneGenerator.js` - 微场景生成
  - `PromptOptimizer.js` - 提示词优化

### 2. 模板简化
- ✅ 删除了 30 个复杂模板
- ✅ 创建了 4 个黑底模板：
  - `BlackBackgroundBasic.jsx` - 基础模板（80%场景）
  - `BlackBackgroundEmphasis.jsx` - 强调模板（15%场景）
  - `BlackBackgroundChart.jsx` - 图表模板（5%场景）
  - `BlackBackgroundKeyword.jsx` - 测试模板

### 3. 数据流修复
- ✅ MicroSceneGenerator 生成 `imageUrl`（豆包图片）
- ✅ MasterAutoGenerationAgent 传递 `imageUrl`
- ✅ RemotionRenderer 接收 `imageUrl` 并传递给模板

### 4. 工作流程
```
视频输入
  ↓
视频分析（提取字幕、关键词）
  ↓
微场景生成（关键词触发 + 豆包生图）
  ↓
模板渲染（黑底模板 + 豆包图片）
  ↓
视频合成（原视频 + 组合场景）
  ↓
最终输出
```

## 🧪 测试步骤

### 前置条件

1. **安装 Python 依赖**
   ```bash
   pip install opencv-python numpy
   ```

2. **启动 Remotion 服务器**
   ```bash
   cd remotion-templates
   npm run server
   ```
   服务器应该运行在 `http://localhost:3002`

3. **准备测试视频**
   - 需要一个包含语音的 MP4 视频
   - 建议时长：30秒 - 2分钟
   - 确保视频包含清晰的语音内容
   - **推荐**：包含人脸的视频（用于测试人脸识别 PIP）

### 运行测试

#### 1. 测试人脸识别（可选但推荐）

```bash
# 测试人脸检测功能
node test-face-detection.js <视频路径>

# 示例
node test-face-detection.js ./my-video.mp4
```

这个测试会：
- 检测视频中的人脸位置
- 计算不同 PIP 尺寸的安全位置
- 测试缓存功能
- 显示可视化结果

#### 2. 测试完整流程

```bash
# 使用测试脚本
node test-simplified-flow.js <视频路径>

# 示例
node test-simplified-flow.js ./my-video.mp4
```

### 预期输出

测试脚本会显示：

1. **进度信息**
   ```
   📊 进度: 10.0% - 视频分析中
   📊 进度: 50.0% - 生成微场景
   📊 进度: 85.0% - 渲染模板
   📊 进度: 100.0% - 合成完成
   ```

2. **结果验证**
   ```
   ✅ 生成完成！
   📦 结果:
     videoUrl: blob:...
     duration: 45.2
     scenes: 8
     keywords: 5

   🔍 验证关键点:
     ✓ 豆包生图: ✅
     ✓ 使用的模板: BlackBackgroundBasic, BlackBackgroundEmphasis
     ✓ 组合场景数量: 3
     ✓ 原视频片段数量: 5
   ```

## 🔍 验证要点

### 1. 人脸识别 PIP 是否工作
- 检查日志中是否有 "🔍 启用人脸检测..."
- 检查是否检测到人脸位置
- 检查 PIP 位置是否避开了人脸
- 无人脸时应该降级到默认位置

### 2. 豆包生图是否工作
- 检查日志中是否有 "🎨 为关键词 XXX 生成图片..."
- 检查是否返回了图片 URL
- 检查图片是否成功加载到模板中

### 2. 模板选择是否正确
- 基础场景应该使用 `BlackBackgroundBasic`
- 重要场景应该使用 `BlackBackgroundEmphasis`
- 数据场景应该使用 `BlackBackgroundChart`

### 3. 微场景生成是否正确
- 关键词出现时应该触发组合场景
- 组合场景持续 3-5 秒
- 其他时间应该显示原视频

### 4. 视频合成是否正确
- 最终视频应该包含原视频片段和组合场景
- 组合场景应该显示 PIP（画中画）效果
- 视频应该流畅播放，无卡顿

## 🐛 常见问题

### 1. Remotion 服务器未启动
```
❌ 错误: fetch failed
```
**解决方案**: 启动 Remotion 服务器
```bash
cd remotion-templates
npm run server
```

### 2. 豆包 API 配置错误
```
❌ 错误: 豆包 API 调用失败
```
**解决方案**: 检查环境变量
```bash
# 确保设置了豆包 API 密钥
export DOUBAO_API_KEY=your_api_key
```

### 2. 视频分析失败
```
❌ 错误: 无法提取字幕
```
**解决方案**:
- 确保视频包含音频
- 检查视频格式是否支持
- 尝试使用其他测试视频

### 3. 人脸检测失败
```
❌ 错误: 人脸检测失败
```
**解决方案**:
- 确保已安装 Python 依赖: `pip install opencv-python numpy`
- 检查 Python 版本（需要 Python 3.6+）
- 检查视频是否包含清晰的人脸
- 人脸检测失败时会自动降级到默认位置，不影响整体流程

### 4. 模板渲染失败
```
❌ 错误: 模板渲染超时
```
**解决方案**:
- 检查 Remotion 服务器日志
- 确保模板文件存在
- 检查传递的参数是否正确

## 📊 性能指标

预期性能（基于 1 分钟视频）：

- **视频分析**: 10-20 秒
- **关键词提取**: 5-10 秒
- **人脸检测**: 1-2 秒/视频（首次），<0.1 秒（缓存）
- **豆包生图**: 每张 3-5 秒
- **模板渲染**: 每个场景 10-15 秒
- **视频合成**: 20-30 秒

**总计**: 约 2-3 分钟

## 🎯 下一步

测试通过后，可以：

1. **优化性能**
   - 并行渲染多个场景
   - 缓存豆包生成的图片
   - 优化视频合成速度

2. **增强功能**
   - 添加更多模板样式
   - 支持自定义品牌色
   - 添加转场效果

3. **用户界面**
   - 创建 Web UI
   - 添加实时预览
   - 支持参数调整

## 📝 测试检查清单

- [ ] Python 依赖已安装（opencv-python, numpy）
- [ ] Remotion 服务器已启动
- [ ] 豆包 API 已配置
- [ ] 测试视频已准备（推荐包含人脸）
- [ ] 运行人脸检测测试
- [ ] 运行完整流程测试
- [ ] 检查人脸识别是否工作
- [ ] 检查 PIP 位置是否避开人脸
- [ ] 检查豆包生图是否工作
- [ ] 检查模板选择是否正确
- [ ] 检查微场景生成是否正确
- [ ] 检查视频合成是否正确
- [ ] 检查最终视频质量
- [ ] 记录性能指标

## 🎉 成功标准

测试成功的标志：

1. ✅ 人脸检测准确识别视频中的人脸
2. ✅ PIP 位置智能避开人脸区域
3. ✅ 所有关键词都触发了豆包生图
4. ✅ 所有组合场景都使用了正确的模板
5. ✅ 最终视频包含原视频和组合场景
6. ✅ 视频播放流畅，无错误
7. ✅ 性能在可接受范围内

---

**最后更新**: 2026-01-20
**版本**: 2.0.0 (新增人脸识别 PIP)

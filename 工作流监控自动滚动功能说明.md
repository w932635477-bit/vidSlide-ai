# 工作流监控台自动滚动功能实现

## 功能概述

为工作流程监控台添加了自动滚动功能，当步骤执行时，绿色完成框会自动滚动到可视区域中心，确保用户始终能看到当前正在执行的步骤。

## 实现细节

### 1. 模板修改 (WorkflowMonitor.vue:28-35)

```vue
<!-- 步骤列表 -->
<div class="monitor-steps" ref="stepsContainer">
  <div
    v-for="(step, index) in steps"
    :key="step.id"
    :ref="el => { if (el) stepRefs[index] = el }"
    class="step-item"
    :class="[`step-${step.status}`]"
  >
```

**关键点：**
- 为容器添加 `ref="stepsContainer"` 引用
- 为每个步骤添加动态 ref：`:ref="el => { if (el) stepRefs[index] = el }"`
- 这样可以在 JavaScript 中访问每个步骤的 DOM 元素

### 2. Script 修改 (WorkflowMonitor.vue:170-312)

#### 2.1 导入必要的 Vue API

```javascript
import { ref, computed, watch, nextTick, onBeforeUpdate } from 'vue'
```

#### 2.2 添加响应式引用

```javascript
const stepsContainer = ref(null)
const stepRefs = ref([])

// 在每次更新前清空 stepRefs
onBeforeUpdate(() => {
  stepRefs.value = []
})
```

#### 2.3 实现自动滚动函数

```javascript
// 自动滚动到当前步骤
const scrollToCurrentStep = async () => {
  await nextTick()

  if (!stepsContainer.value || props.currentStepIndex < 0) return

  const currentStepElement = stepRefs.value[props.currentStepIndex]
  if (!currentStepElement) return

  // 使用 scrollIntoView 平滑滚动到当前步骤
  currentStepElement.scrollIntoView({
    behavior: 'smooth',
    block: 'center',
    inline: 'nearest'
  })
}
```

**关键配置：**
- `behavior: 'smooth'` - 平滑滚动动画
- `block: 'center'` - 将元素滚动到可视区域中心
- `inline: 'nearest'` - 水平方向最小滚动

#### 2.4 添加监听器

```javascript
// 监听当前步骤索引变化
watch(() => props.currentStepIndex, (newIndex) => {
  if (newIndex >= 0 && newIndex < props.steps.length) {
    const currentStep = props.steps[newIndex]
    if (currentStep) {
      currentStep.expanded = true
    }
  }

  // 滚动到当前步骤
  scrollToCurrentStep()
})

// 监听步骤状态变化
watch(() => props.steps.map(s => s.status), () => {
  scrollToCurrentStep()
}, { deep: true })
```

### 3. 样式增强 (WorkflowMonitor.vue:403-432)

#### 3.1 正在执行的步骤 - 脉冲动画

```css
.step-item.step-running {
  border-color: #4a9eff;
  box-shadow: 0 0 8px rgba(74, 158, 255, 0.3);
  animation: pulse-border 2s ease-in-out infinite;
}

@keyframes pulse-border {
  0%, 100% {
    box-shadow: 0 0 8px rgba(74, 158, 255, 0.3);
  }
  50% {
    box-shadow: 0 0 16px rgba(74, 158, 255, 0.6);
  }
}
```

#### 3.2 完成的步骤 - 淡入动画

```css
.step-item.step-success {
  border-color: #4ec9b0;
  animation: fade-in 0.3s ease-out;
}

@keyframes fade-in {
  from {
    opacity: 0.5;
    transform: translateX(-4px);
  }
  to {
    opacity: 1;
    transform: translateX(0);
  }
}
```

## 功能特性

### ✅ 自动滚动
- 当步骤开始执行时，自动滚动到可视区域中心
- 使用平滑动画，用户体验流畅

### ✅ 视觉反馈
- 正在执行的步骤：蓝色边框 + 脉冲动画
- 完成的步骤：绿色边框 + 淡入动画
- 待执行的步骤：灰色边框 + 半透明

### ✅ 自动展开
- 当前步骤自动展开，显示详细信息
- 包括输入数据、执行日志、输出数据

### ✅ 响应式
- 监听步骤索引变化
- 监听步骤状态变化
- 实时更新滚动位置

## 测试文件

创建了测试页面：`vidslide-ai/public/workflow-monitor-scroll-test.html`

### 测试步骤：
1. 在浏览器中打开测试文件
2. 点击"▶️ 开始执行"按钮
3. 观察步骤自动滚动效果
4. 查看动画和视觉反馈

### 测试场景：
- 10 个步骤的工作流
- 每个步骤执行 1-2 秒
- 自动滚动到当前步骤
- 完成后显示绿色边框

## 技术要点

### 1. Vue 3 Composition API
- 使用 `ref` 创建响应式引用
- 使用 `watch` 监听数据变化
- 使用 `nextTick` 确保 DOM 更新

### 2. 动态 Ref
```javascript
:ref="el => { if (el) stepRefs[index] = el }"
```
- 为列表中的每个元素创建引用
- 使用函数形式的 ref
- 在 `onBeforeUpdate` 中清空旧引用

### 3. scrollIntoView API
- 原生浏览器 API
- 支持平滑滚动
- 可配置滚动位置

### 4. CSS 动画
- `@keyframes` 定义动画
- `animation` 属性应用动画
- 使用 `ease-in-out` 缓动函数

## 浏览器兼容性

- ✅ Chrome 61+
- ✅ Firefox 36+
- ✅ Safari 15.4+
- ✅ Edge 79+

`scrollIntoView` 的 `behavior: 'smooth'` 在旧版浏览器中会降级为立即滚动。

## 性能优化

1. **使用 nextTick**：确保 DOM 更新后再滚动
2. **条件检查**：避免不必要的滚动操作
3. **CSS 动画**：使用 GPU 加速的 transform 和 opacity
4. **防抖处理**：watch 监听器自动防抖

## 未来改进

- [ ] 添加滚动速度配置
- [ ] 支持手动滚动时暂停自动滚动
- [ ] 添加滚动位置记忆功能
- [ ] 支持键盘快捷键导航

## 相关文件

- `vidslide-ai/src/components/WorkflowMonitor.vue` - 主组件
- `vidslide-ai/public/workflow-monitor-scroll-test.html` - 测试页面
- `vidslide-ai/src/views/WorkspaceView.vue` - 使用该组件的视图

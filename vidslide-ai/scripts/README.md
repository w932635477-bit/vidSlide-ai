# VidSlide AI 开发工具集

这个目录包含了VidSlide AI项目的自动化开发工具，用于确保代码质量和开发效率。

## 📋 工具概览

### 1. 约束检查器 (`constraint-checker.js`)

**功能**: 自动检查代码是否符合`.cursor-constraints.md`约束文档

**使用方法**:

```bash
# 检查整个项目
npm run check-constraints

# 或直接运行
node scripts/constraint-checker.js
```

**检查内容**:

- ✅ 技术栈合规性 (Vue 3, Pinia等)
- ✅ 测试覆盖率 (≥80%)
- ✅ 代码质量 (ESLint, 注释率)
- ✅ 功能范围控制
- ✅ 开发阶段合规性

### 2. 约束度量工具 (`constraint-metrics.js`)

**功能**: 统计和分析约束合规情况，生成趋势报告

**使用方法**:

```bash
# 查看合规性报告
npm run constraint-report

# 重置度量数据
npm run constraint-reset

# 或直接运行
node scripts/constraint-metrics.js report
node scripts/constraint-metrics.js reset
```

**功能特性**:

- 📊 合规率统计
- 📈 趋势分析
- 🎯 问题分类
- 💡 改进建议

### 3. 智能提醒系统 (`smart-reminder.js`)

**功能**: 基于开发阶段和优先级提供智能提醒

**使用方法**:

```bash
# 每日开发提醒
npm run dev-reminder

# 每周任务提醒
npm run weekly-reminder

# 或直接运行
node scripts/smart-reminder.js daily   # 每日提醒
node scripts/smart-reminder.js         # 每周提醒
node scripts/smart-reminder.js progress # 进度查看
node scripts/smart-reminder.js tasks   # 任务列表
```

**提醒内容**:

- 📅 当前开发阶段信息
- ⚡ 优先级任务提醒
- 📝 具体开发任务
- 🧪 质量要求
- ⚠️ 风险警告

## 🚀 集成工作流

### 每日开发流程

```bash
# 1. 开始开发前 - 查看今日提醒
npm run dev-reminder

# 2. 开发过程中 - 约束检查
npm run check-constraints

# 3. 提交代码前 - 自动检查 (pre-commit hook)
git add .
git commit -m "feat: 完成XXX功能"
# pre-commit 会自动运行约束检查
```

### 每周开发流程

```bash
# 周一 - 查看本周任务和提醒
npm run weekly-reminder

# 周中 - 检查开发进度
node scripts/smart-reminder.js progress

# 周五 - 生成约束合规报告
npm run constraint-report
```

## 📊 工具配置

### 约束检查器配置

在 `constraint-checker.js` 中可以自定义检查规则：

```javascript
// 检查规则配置
const CHECK_RULES = {
  minTestCoverage: 80,        // 最低测试覆盖率
  minCommentRate: 20,        // 最低注释率
  allowedDeps: [...],        // 允许的依赖
  forbiddenDeps: [...],      // 禁止的依赖
};
```

### 度量工具配置

度量数据存储在项目根目录的 `.constraint-metrics.json`：

```json
{
  "startDate": "2026-01-05T00:00:00.000Z",
  "totalChecks": 25,
  "complianceRate": 92,
  "history": [...]
}
```

## 🔧 扩展开发

### 添加新的检查规则

1. 在 `constraint-checker.js` 中添加新的检查方法：

```javascript
async checkNewRule() {
  // 实现新的检查逻辑
  // 返回 violations 或 warnings
}
```

2. 在 `checkAll()` 方法中调用新的检查：

```javascript
await this.checkNewRule()
```

### 自定义提醒内容

在 `smart-reminder.js` 中可以自定义提醒内容：

```javascript
// 添加新的阶段配置
const stageConfig = {
  'new-stage': {
    name: '新阶段',
    period: 'Week XX-XX'
    // ...
  }
}
```

## 📈 最佳实践

### 1. 坚持每日检查

- 每天开始开发前运行 `npm run dev-reminder`
- 每次修改重要代码后运行 `npm run check-constraints`

### 2. 定期查看报告

- 每周查看 `npm run constraint-report`
- 关注合规率趋势和常见问题

### 3. 主动改进

- 根据工具建议改进代码质量
- 关注约束违反的趋势变化
- 持续优化开发流程

### 4. 团队协作

- 所有团队成员都应熟悉这些工具
- 在代码审查中包含约束检查结果
- 定期分享改进经验

## 🆘 故障排除

### 约束检查失败

```
问题: npm run check-constraints 报告违反约束
解决:
1. 查看具体违反内容
2. 根据建议修复问题
3. 重新运行检查
```

### 度量数据异常

```
问题: 报告数据不准确
解决:
1. npm run constraint-reset 重置数据
2. 重新开始收集度量数据
```

### 提醒信息过时

```
问题: 阶段信息不匹配当前进度
解决:
1. 检查 smart-reminder.js 中的阶段计算逻辑
2. 更新阶段配置或计算方法
```

## 📞 支持

如果遇到工具使用问题，请：

1. 查看工具的错误输出信息
2. 检查 `scripts/` 目录下的工具代码
3. 参考本 README 的故障排除部分
4. 在项目 issue 中提出问题

---

**这些工具将帮助VidSlide AI项目保持高质量的开发标准，确保每个功能都符合约束要求，逐步实现"30秒将视频转为专业PPT"的核心价值！** 🎯✨
